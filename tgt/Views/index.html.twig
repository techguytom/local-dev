<!DOCTYPE html>
<html>
<body>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>{{ title }}</title>
    <meta name="viewport" content="width=device-width">
    <link rel="stylesheet" type="text/css" href="css/main.css"/>
    <div class="canvas">
        <header>
            <h1>{{ title }} PHP v{{ phpVersion }}</h1>
            <nav>
                <ul>
                    {% for site in nav %}
                        <a href="{{ site['url'] }}" target="_blank">
                            <li>{{ site['name'] }}</li>
                        </a>
                    {% endfor %}
                </ul>
            </nav>
        </header>
        <content>
            <ul class="sites">
                {% for site in sites %}
                    {% if site['rails'] is defined %}
                        <li><a href="http://localhost:3000" target="_blank">
                                {% if site['favicon'] is defined %}
                                    <img src="{{ site['favicon'] }}"/>
                                {% endif %}
                                {{ site['siteName'] }} development</a>
                        </li>
                    {% else %}
                        <li><a href="http://{{ site['siteName'] }}.localhost{{ site['symfony'] }}" target="_blank">
                                {% if site['favicon'] is defined %}
                                    <img src="{{ site['favicon'] }}"/>
                                {% endif %}
                                {{ site['siteName'] }} development</a>
                            {% if site['prod'] is defined %}
                                &nbsp;|&nbsp;<a href="http://{{ site['siteName'] }}.prod" target="_blank">
                                {% if site['favicon'] is defined %}
                                    <img src="{{ site['favicon'] }}"/>
                                {% endif %}
                                {{ site['siteName'] }} production</a>
                            {% endif %}
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>
        </content>
    </div>
</body>
</html>
